// Shared, client-safe constants (no server-only imports here).

export const SESSION_COOKIE = 'aegis_user'

// The single demo credential that unlocks the fully-populated sample vault.
// Any other email/password combination signs in to a fresh, empty vault.
export const DEMO_EMAIL = 'ravi@aegis.app'
export const DEMO_PASSWORD = 'demo1234'
