// Shared, client-safe constants (no server-only imports here).
//
// Authentication is handled by Better Auth (lib/auth.ts); it manages its own
// session cookie, so there are no auth constants to share here.

export {}
